# Scripts

Placeholder for adopters to add stack-specific audits, for example:

- `production-check` — walks `checklists/production.md` against a repo
- `dependency-audit` — npm/pnpm audit wrapper
- `security-audit` — secret scan + basic header checks

This documentation repo keeps scripts minimal on purpose.
